<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moveit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".views.BookingRecordActivity"></activity>
        <activity android:name=".views.TearmsconditionActivity" />
        <activity android:name=".views.AboutUsActivity" />
        <activity android:name=".views.ContactUsActivity" />
        <activity android:name=".views.FeedbackActivity" />
        <activity android:name=".views.ProfileActivity" />
        <activity android:name=".views.RouteActivity" />
        <activity android:name=".views.Forgot_PasswordActivity" />
        <activity android:name=".views.ChangePasswordActivity" />
        <activity android:name=".views.RegistrationActivity" />
        <activity android:name=".views.LoginActivity" />
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>